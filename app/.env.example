# LEGACY MIGRATION INPUT ONLY. MaintBot no longer reads public settings from
# this file at runtime. Current settings live in data/conf/bot.json and each
# server lives in its own data/conf/servers/*.json file. Keep this template only
# for tools/migrate_runtime_env.py -> tools/migrate_config_layout.py upgrades.
# Secrets still belong in app/env.secrets (or SECRETS_ENV_PATH).

# ---------------------------------------------------------------------------
# General runtime
# ---------------------------------------------------------------------------
TZ=Europe/Moscow
LOG_LEVEL=INFO
LOG_JSON=false
DATA_DIR=/opt/maintbot/data
SERVER_INVENTORY_FILE=/etc/maintbot/servers.toml
PTB_PERSISTENCE_PATH=/opt/maintbot/data/telegram/persistence.pickle
INSTANCE_LOCK_PATH=/run/maintbot/maintbot.lock

# ---------------------------------------------------------------------------
# Authentication and background delivery
# ---------------------------------------------------------------------------
AUTH_FAIL_WINDOW_SEC=300
AUTH_MAX_FAILS_IN_WINDOW=5
AUTH_GLOBAL_MAX_FAILS_IN_WINDOW=40
AUTH_LOCKOUT_SEC=600
AUTH_PRUNE_INTERVAL_SEC=300
ACCESS_REQUEST_COOLDOWN_SEC=300
ERROR_NOTIFY_INTERVAL_SEC=300
OUTBOX_PROCESS_INTERVAL_SEC=10

# ---------------------------------------------------------------------------
# Navigation cleanup (messages such as broadcasts are never cleanup targets)
# ---------------------------------------------------------------------------
NAVIGATION_CLEANUP_ENABLED=true
NAVIGATION_RETENTION_HOURS=24
NAVIGATION_CLEANUP_INTERVAL_SEC=1800

# ---------------------------------------------------------------------------
# DNS and status cache
# ---------------------------------------------------------------------------
DNS_RESOLVERS=1.1.1.1,8.8.8.8,77.88.8.8
DNS_DAILY_REFRESH_AT=03:05
DNS_STARTUP_REFRESH_DELAY_SEC=5
STATUS_CACHE_TTL_SEC=5
DAILY_NODE_STATUS_REFRESH_AT=12:00

# ---------------------------------------------------------------------------
# Remnawave metrics endpoint. Credentials are stored in env.secrets.
# Node UUIDs and monitoring source are configured per server in servers.toml.
# ---------------------------------------------------------------------------
REMNAWAVE_METRICS_URL=
REMNAWAVE_METRICS_TIMEOUT_SEC=3
REMNAWAVE_METRICS_CACHE_TTL_SEC=8
REMNAWAVE_METRICS_MAX_BYTES=2000000
REMNAWAVE_HIDDEN_UUIDS=

# ---------------------------------------------------------------------------
# SSH and privileged local helper
# ---------------------------------------------------------------------------
SSH_STRICT_HOST_KEY_CHECKING=yes
SSH_IDENTITY_FILE=/etc/maintbot/ssh/id_ed25519
SSH_KNOWN_HOSTS_FILE=/etc/maintbot/ssh/known_hosts
PRIVILEGED_HELPER_BIN=/usr/local/libexec/maintbot-helper
SUBPROC_SHORT_TIMEOUT=3
SUBPROC_MEDIUM_TIMEOUT=8
SUBPROC_MAX_OUTPUT_BYTES=1000000

# ---------------------------------------------------------------------------
# Scheduled summaries
# ---------------------------------------------------------------------------
FAIL2BAN_DAILY_AT=12:00
FAIL2BAN_DIGEST_TAIL_LINES=20000
FAIL2BAN_DIGEST_MAX_BYTES=3000000
MAINT_RESTART_NOTIFY_DELAY_SEC=2
MAINT_RESTART_REMINDER_INTERVAL_SEC=1800
