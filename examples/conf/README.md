# MaintBot configuration examples

Every server name, label, hostname, address, UUID, and account in this directory is synthetic and
reserved for documentation. Copy these files to `data/conf/`, then replace the
placeholders with the settings for the target installation. Runtime code never
loads configuration directly from this directory.
